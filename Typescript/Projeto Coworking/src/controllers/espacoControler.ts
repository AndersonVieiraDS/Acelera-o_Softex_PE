// src/controllers/spaceController.ts
import { Request, Response } from 'express';
import { Espaco } from '../models/espacoModels';

export const getAllSpaces = (req: Request, res: Response) => {
  // Lógica para obter todos os espaços
};

// Outros controladores para criar, atualizar e excluir espaços
